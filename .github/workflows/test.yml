on: push
jobs:
  test:
    runs-on: ubuntu-24.04
    steps:
      - uses: docker://public.ecr.aws/lambda/python:3.14
        with:
          entrypoint: /bin/sh -c
          args: env
        run: env
